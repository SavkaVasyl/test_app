# test_app

# Basic project setup
1. Get all dependencies
```
flutter pub get
```
2. Run code generation (generate api calls and models)
```
dart run build_runner build --delete-conflicting-outputs
```
3. Run the app
```
flutter run
```